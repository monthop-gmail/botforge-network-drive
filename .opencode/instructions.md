# Project Context

**Working Directory:** `/workspace/projects/botforge-network-drive`

## Git Operations Rules
- ALWAYS verify current directory with `pwd` before any git command
- ALWAYS run `git remote -v` to confirm correct repository before push
- NEVER run git commands from /workspace root
- This is an independent repository - do not mix with other projects

## Quick Check
Before git push:
1. `pwd` → must show `/workspace/projects/botforge-network-drive`
2. `git status` → must show correct repo
3. `git remote -v` → must show correct remote URL
